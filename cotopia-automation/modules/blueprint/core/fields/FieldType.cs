namespace blueprint.modules.blueprint.core.fields
{
    public enum FieldType
    {
        @string,
        @int,
        @double,
        @bool,
        @dropdown,
        @node,
        @variable,
    }
}
